{
  "name": "afm-json-bridge",
  "version": "0.1.0",
  "description": "Convert font metrics between Adobe AFM and a JSON representation",
  "license": "MIT",
  "type": "module",
  "main": "dist/afm.js",
  "types": "dist/afm.d.ts",
  "bin": {
    "afm-json-bridge": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test"
  }
}
